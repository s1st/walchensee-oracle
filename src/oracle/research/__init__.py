"""Offline research spikes (not imported by prod paths).

Modules here explore questions like "can intraday meteo predict thermal *onset
time*?" against the historical archive. They may pull the [ml] extra and the
Open-Meteo archive; nothing in `oracle.engine` / the Dockerfiles imports them.
"""
